using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using cakeslice;

public class HexagonOutline : MonoBehaviour {

    //Outline outline;
    //Hexagon hex;

    //private void Start()
    //{
    //    outline = GetComponent<Outline>();
    //    outline.enabled = false;
    //    hex = GameManager.instance.gridReference.GetHexagonFromWorldPosition(transform.position);
    //}

    //private void OnMouseEnter()
    //{
    //    if(GameManager.instance.currentActivePlayer.currentAction == PlayerController.Action.placeCard && !GameManager.instance.currentActivePlayer.hasPlacedCard)
    //    {           
    //        if (GameManager.instance.currentActivePlayer.selectedCard && hex.card == null)
    //        {
    //            outline.enabled = true;
    //            outline.color = 2;
    //        }
    //    }
    //}

}
